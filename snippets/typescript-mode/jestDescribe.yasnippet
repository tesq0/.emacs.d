# -*- mode: snippet -*-
# name: jestDescribe
# key: describe
# --
describe('${1:`(concat (file-basename (buffer-file-name)) " " "test")`}', () => {
  it('${2:should produce correct result}', () => {
    expect($0);
  });
});
